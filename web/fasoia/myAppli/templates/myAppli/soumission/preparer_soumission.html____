{% extends "base_app.html" %}
{% load static %}

{% block title %}FASOIA — Préparation de Dossier{% endblock %}
{% block breadcrumb %}Préparation Soumission{% endblock %}

{% block app_extra_css %}
<style>
    /* ===== EN-TÊTE SPECIFIQUE SOUMISSION ===== */
    .dash-header {
        background: var(--ink);
        padding: 28px 32px;
        margin-bottom: 24px;
        display: flex;
        align-items: flex-start;
        justify-content: space-between;
        gap: 20px;
        flex-wrap: wrap;
        border-bottom: 3px solid var(--red);
    }
    .dash-header-left h1 {
        font-family: var(--font-display);
        font-size: 26px;
        font-weight: 700;
        color: white;
        line-height: 1.2;
        margin-bottom: 10px;
    }
    .dash-header-left h1 span { color: var(--red); }

    .dash-tag-ref {
        display: inline-flex;
        align-items: center;
        gap: 6px;
        padding: 4px 12px;
        background: rgba(255,255,255,0.05);
        border: 1px solid var(--red);
        color: var(--red);
        font-family: monospace;
        font-size: 12px;
        font-weight: 700;
    }

    .dash-header-right { text-align: right; flex-shrink: 0; }
    .dash-completion-num {
        font-family: var(--font-display);
        font-size: 48px;
        font-weight: 700;
        color: white;
        line-height: 1;
    }
    .dash-completion-num span { color: var(--red); }
    
    .dash-prog-track {
        height: 4px;
        background: rgba(255,255,255,0.1);
        margin-top: 10px;
        width: 160px;
        margin-left: auto;
        overflow: hidden;
    }
    .dash-prog-fill {
        height: 100%;
        background: var(--red);
        width: {{ progression.pourcentage }}%;
        transition: width 0.8s cubic-bezier(0.4, 0, 0.2, 1);
    }

    /* ===== CARTES & TABLES STYLE FASOIA ===== */
    .f-card {
        background: var(--surface);
        border: 1px solid var(--border);
        margin-bottom: 24px;
    }
    .f-card-head {
        background: var(--ink);
        padding: 14px 20px;
        display: flex;
        align-items: center;
        justify-content: space-between;
    }
    .f-card-head h3 {
        color: white;
        font-family: var(--font-display);
        font-size: 14px;
        font-weight: 700;
        text-transform: uppercase;
        letter-spacing: 1px;
        margin: 0;
    }

    /* Table des documents */
    .f-table {
        width: 100%;
        border-collapse: collapse;
    }
    .f-table th {
        background: var(--bg);
        color: var(--muted);
        padding: 12px 20px;
        text-align: left;
        font-size: 10px;
        font-weight: 700;
        text-transform: uppercase;
        border-bottom: 1px solid var(--border);
    }
    .f-table td {
        padding: 16px 20px;
        border-bottom: 1px solid var(--border);
        font-size: 13px;
        vertical-align: middle;
    }
    .f-table tr:hover { background: var(--cream); }

    /* Status Badges FASOIA */
    .f-badge {
        padding: 4px 10px;
        font-size: 10px;
        font-weight: 700;
        text-transform: uppercase;
        display: inline-block;
    }
    .f-badge-wait { background: var(--amber); color: white; }
    .f-badge-done { background: var(--teal); color: white; }

    /* Boutons FASOIA */
    .btn-f {
        padding: 8px 16px;
        font-size: 11px;
        font-weight: 700;
        text-transform: uppercase;
        border: none;
        cursor: pointer;
        display: inline-flex;
        align-items: center;
        gap: 8px;
        text-decoration: none;
        transition: 0.2s;
    }
    .btn-f-red { background: var(--red); color: white; }
    .btn-f-ink { background: var(--ink); color: white; }
    .btn-f-outline { background: transparent; border: 1px solid var(--ink); color: var(--ink); }
    .btn-f:hover { opacity: 0.9; transform: translateY(-1px); }
    .btn-f:disabled { background: var(--border); color: var(--muted); cursor: not-allowed; }

    /* ===== ALERTES ===== */
    .f-alert {
        padding: 15px 20px;
        border-left: 4px solid var(--red);
        background: #fff5f5;
        font-size: 12px;
        color: #862323;
        margin-bottom: 20px;
    }

    .doc-icon {
        font-size: 18px;
        color: var(--ink);
        margin-right: 12px;
    }
</style>
{% endblock %}

{% block content %}
<div class="dash-header">
    <div class="dash-header-left">
        <h1>Préparation : <span>{{ opportunite.titre|truncatechars:80 }}</span></h1>
        <div class="dash-tags">
            <div class="dash-tag-ref">
                <i class="fa fa-file-invoice"></i> RÉF: {{ dossier.id }}
            </div>
            <div class="dash-tag" style="background: white; border: 1px solid var(--border);">
                <i class="fa fa-calendar-alt"></i> Date limite : <b style="color:var(--red)">{{ opportunite.date_limite|date:"d/m/Y" }}</b>
            </div>
        </div>
    </div>
    <div class="dash-header-right">
        <div class="dash-completion-num">{{ progression.pourcentage }}<span>%</span></div>
        <div class="dash-completion-lbl">Dossier complété</div>
        <div class="dash-prog-track">
            <div class="dash-prog-fill"></div>
        </div>
    </div>
</div>

<div class="row">
    <div class="col-lg-8">
        
        <div class="f-card">
            <div class="f-card-head">
                <h3>1. Générer les pièces requises (Templates)</h3>
            </div>
            <table class="f-table">
                <thead>
                    <tr>
                        <th>Nom du document</th>
                        <th style="width: 150px;">Action</th>
                    </tr>
                </thead>
                <tbody>
                    {% for modele in modeles %}
                    <tr>
                        <td>
                            <div class="d-flex align-items-center">
                                <i class="fa fa-file-word doc-icon"></i>
                                <div>
                                    <div class="fw-bold text-ink">{{ modele.nom }}</div>
                                    <div class="text-muted" style="font-size: 11px;">Génération automatique FASOIA</div>
                                </div>
                            </div>
                        </td>
                        <td>
                            <a href="{% url 'myAppli:generer_document' dossier.id modele.id %}" class="btn-f btn-f-red">
                                <i class="fa fa-sync"></i> Générer
                            </a>
                        </td>
                    </tr>
                    {% empty %}
                    <tr>
                        <td colspan="2" class="text-center py-4 text-muted">Aucun modèle configuré.</td>
                    </tr>
                    {% endfor %}
                </tbody>
            </table>
        </div>

        <div class="f-card">
            <div class="f-card-head">
                <h3>2. Vérifier et Valider les fichiers</h3>
            </div>
            <table class="f-table">
                <thead>
                    <tr>
                        <th>Fichier généré</th>
                        <th>Statut</th>
                        <th class="text-right">Actions</th>
                    </tr>
                </thead>
                <tbody>
                    {% for doc in documents %}
                    <tr>
                        <td>
                            <i class="fa fa-check-circle {% if doc.statut == 'VALIDE' %}text-success{% else %}text-muted{% endif %} mr-2"></i>
                            <span class="text-ink fw-bold">{{ doc.nom_document }}</span>
                            <br><small class="text-muted ml-4">Généré le {{ doc.date_generation|date:"d/m H:i" }}</small>
                        </td>
                        <td>
                            {% if doc.statut == 'VALIDE' %}
                                <span class="f-badge f-badge-done">Validé</span>
                            {% else %}
                                <span class="f-badge f-badge-wait">Brouillon</span>
                            {% endif %}
                        </td>
                        <td class="text-right">
                            <a href="{{ doc.fichier_genere.url }}" class="btn-f btn-f-outline" title="Télécharger">
                                <i class="fa fa-download"></i>
                            </a>
                            {% if doc.statut != 'VALIDE' %}
                                <button onclick="validerDocument({{ doc.id }})" class="btn-f btn-f-ink" title="Valider">
                                    <i class="fa fa-check"></i>
                                </button>
                            {% endif %}
                        </td>
                    </tr>
                    {% empty %}
                    <tr>
                        <td colspan="3" class="text-center py-5">
                            <div class="text-muted small">Aucun document n'a été généré pour le moment.</div>
                        </td>
                    </tr>
                    {% endfor %}
                </tbody>
            </table>
        </div>
    </div>

    <div class="col-lg-4">
        <div class="f-card" style="position: sticky; top: 20px;">
            <div class="f-card-head">
                <h3>Finalisation</h3>
            </div>
            <div class="p-4 bg-white">
                <div class="mb-4">
                    <div class="d-flex justify-content-between mb-2">
                        <span class="text-muted small uppercase fw-bold">Progression</span>
                        <span class="fw-bold text-ink">{{ progression.valides }}/{{ progression.total }}</span>
                    </div>
                    <div class="dash-prog-track" style="width: 100%; margin-left: 0; background: #eee;">
                        <div class="dash-prog-fill"></div>
                    </div>
                </div>

                {% if progression.complet %}
                    <div class="f-alert" style="border-left-color: var(--teal); background: #f0fff4; color: #166534;">
                        <i class="fa fa-check-circle"></i> Tous les documents APEX-B sont validés. Le dossier est prêt.
                    </div>
                    <a href="{% url 'myAppli:soumettre_dossier' dossier.id %}" class="btn-f btn-f-red w-100 py-3 d-flex justify-content-center" style="font-size: 14px;">
                        <i class="fa fa-paper-plane"></i> Soumettre à l'APEX-B
                    </a>
                {% else %}
                    <div class="f-alert">
                        <i class="fa fa-exclamation-triangle"></i> 
                        <b>Action requise :</b> Vous devez générer et valider tous les documents requis avant de pouvoir soumettre.
                    </div>
                    <button class="btn-f btn-f-ink w-100 py-3 d-flex justify-content-center" disabled style="opacity: 0.5;">
                        <i class="fa fa-lock"></i> Soumission Verrouillée
                    </button>
                {% endif %}

                <div class="mt-4 pt-3 border-top">
                    <p class="text-muted" style="font-size: 11px; line-height: 1.5;">
                        <i class="fa fa-info-circle"></i> 
                        En cliquant sur "Soumettre", FASOIA compilera vos pièces validées et les transmettra via le portail ou générera l'archive finale.
                    </p>
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}

{% block extra_js %}
<script>
function validerDocument(docId) {
    // Utilisation de Fetch API pour la validation AJAX
    fetch(`/soumission/document/${docId}/valider/`, {
        method: 'POST',
        headers: {
            'X-CSRFToken': '{{ csrf_token }}',
            'Content-Type': 'application/json'
        }
    }).then(response => {
        if (response.ok) {
            // Rechargement fluide pour mettre à jour la progression
            window.location.reload();
        } else {
            alert('Erreur lors de la validation du document.');
        }
    });
}
</script>
{% endblock %}